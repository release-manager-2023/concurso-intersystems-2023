import { Route } from '@angular/router';
import { StakeholderFormComponent } from './stakeholder-form/stakeholder-form.component';

export const stakeholderFormRoutes: Route[] = [{ path: '', component: StakeholderFormComponent }];
